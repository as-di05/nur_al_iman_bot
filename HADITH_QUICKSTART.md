# Быстрый старт: Хадисы

## Первый запуск

После запуска бота функционал хадисов готов к использованию. Но база данных пуста.

## Шаг 1: Создать коллекцию (только @as_di05)

В Telegram:
```
/hadith_admin
→ Нажать "➕ Создать коллекцию"
→ Отправить название: Сахих Бухари
```

✅ Коллекция создана! Скопируйте ID из ответа.

## Шаг 2: Добавить первый хадис

```
/hadith_admin
→ Нажать "➕ Добавить хадис"
→ Выбрать "Сахих Бухари"
→ Отправить номер: 1
→ Отправить текст хадиса на русском
```

✅ Хадис добавлен! Можно добавлять следующие.

## Шаг 3: Дополнить хадис (опционально)

Чтобы добавить арабский текст, передатчиков или разъяснения:

```
/hadith_admin
→ Нажать "✏️ Редактировать хадис"
→ Отправить ID хадиса (из предыдущего шага)
→ Выбрать поле для редактирования
→ Отправить новое значение
```

## Шаг 4: Подписаться на хадисы (пользователи)

Любой пользователь может подписаться:

```
/hadith
→ Выбрать коллекцию "Сахих Бухари"
```

✅ Подписка активна! Хадисы будут приходить ежедневно в 09:00.

## Поиск хадиса

```
/hadith_admin
→ Нажать "🔍 Поиск хадиса"
→ Выбрать коллекцию
→ Отправить номер хадиса
```

Покажет полную информацию о хадисе включая ID.

## Просмотр всех коллекций

```
/hadith_admin
→ Нажать "📚 Список коллекций"
```

Покажет все коллекции с количеством хадисов и ID.

## Важные заметки

- Минимум для добавления хадиса: **номер + текст на русском**
- Остальное можно добавить позже через редактирование
- Хадисы отправляются по порядку (поле `order`, автоматически)
- После последнего хадиса цикл начинается с начала
- Каждый пользователь имеет свой прогресс чтения
- Время отправки: **09:00** (Asia/Bishkek, пока не настраивается)

## Тестирование

Чтобы протестировать отправку без ожидания 09:00, можно временно изменить время в файле:

`src/services/hadithScheduler.js` строка 11:
```javascript
"0 9 * * *"  // Изменить на нужное время, например "30 14 * * *" для 14:30
```

Не забудьте вернуть обратно после тестирования!

## Массовое добавление хадисов

Для добавления большого количества хадисов рекомендуется:

1. Подготовить список в формате:
   ```
   Номер: Текст хадиса
   ```

2. Использовать последовательно команду добавления

3. Или написать скрипт для прямого добавления в MongoDB (в будущем можно добавить импорт из JSON)

## Деактивация хадиса

Если нужно временно скрыть хадис:

```
/hadith_admin
→ "✏️ Редактировать хадис"
→ Ввести ID
→ Нажать "🔴 Деактивировать"
```

Хадис не будет отправляться, но останется в базе.

## Поддержка

При возникновении проблем проверьте логи:
```bash
./run.sh logs
```

Ищите строки с префиксом `📖` для отслеживания отправки хадисов.
